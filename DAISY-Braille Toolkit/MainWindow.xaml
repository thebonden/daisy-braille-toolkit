<Window x:Class="DAISY_Braille_Toolkit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DAISY-Braille Toolkit" Height="520" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ElevenLabs API key (saved locally, encrypted with Windows DPAPI) -->
        <GroupBox Header="ElevenLabs API" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="API key:"/>
                <PasswordBox Grid.Row="0" Grid.Column="1" x:Name="ApiKeyBox" Margin="0,0,10,0"/>

                <Button Grid.Row="0" Grid.Column="2" Width="120" Margin="0,0,10,0"
                        Content="Gem lokalt" Click="SaveApiKey_Click"/>
                <Button Grid.Row="0" Grid.Column="3" Width="120"
                        Content="Slet lokalt" Click="DeleteApiKey_Click"/>

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Status:"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ApiKeyStatusText" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </GroupBox>

        <!-- Placeholder for future modules -->
        <GroupBox Grid.Row="1" Header="Status" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="Her kommer konverteringsmoduler (DAISY, PEF, ISO, CSV) senere."/>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Log" Padding="10">
            <TextBox x:Name="LogBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        </GroupBox>
    </Grid>
</Window>
